# v3 の板（チケット一覧）

**1 チケット = 1 セッション。** 着手するときに読むのは **この板と、そのチケット 1 枚だけ**でよい。
チケットは単体で読めるように書いてある（必要な前提はチケットの中に写してある）。
`docs/plan-v3.md` と dev-notes は**読まなくてよい**（迷ったときだけ、チケットが指す節を開く）。

段は 2 つ: **「形を探す」**（STO-3G/LDA、今のまま、既定）と **「形を測る」**（6-31G\*/LDA）。
コード上の名前は `ModelLevel = 'shape' | 'measure'`。

| # | 題 | 依存 | 状態 |
| --- | --- | --- | --- |
| [V3-1](V3-1.md) | エンジンが基底を選べるようにする（Rust） | — | 完了（2026-09-21） |
| [V3-2](V3-2.md) | 6-31G\* の参照値とテスト | V3-1 | 完了（2026-09-21） |
| [V3-3](V3-3.md) | 段を wasm 境界と Worker の契約に通す | V3-1 | 完了（2026-09-21） |
| [V3-4](V3-4.md) | 計算ごとに段を選ぶ UI | V3-3 | 完了（2026-09-22） |
| [V3-5](V3-5.md) | 記録が段を持つ | V3-3 | 未着手 |
| [V3-6](V3-6.md) | 探索は「形を探す」に固定する | V3-3 | 未着手 |
| [V3-7](V3-7.md) | 中断してもそこまでの形と数値を残す | V3-3 | 未着手 |
| [V3-8](V3-8.md) | 速さとメモリの実測、予算の見直し | V3-4〜V3-6 | 未着手 |
| [V3-9](V3-9.md) | ユーザーの確認（Firefox・講義 PC） | 全部 | 未着手 |

状態は `未着手` / `作業中` / `完了（日付）`。

## チケットを終えるときにやること

1. **この板の状態を書き換える**（1 行）。
2. **実測・却下した案・数字の出どころは dev-notes の「フェーズの記録」に 1 節**足す。
   チケット本文には書かない（チケットは着手のための紙で、記録ではない）。
3. **規約が増えたら CLAUDE.md に 1〜2 行**。増えていないなら触らない。
4. コミットはユーザーに言われたときだけ。

## 全チケットに共通する前提

- **単位**: エンジンは Bohr/Hartree、UI は Å。変換は `dft-wasm` の境界だけ。
- **F4**: 画面に基底・汎関数・電荷・多重度を出さない。段の名前も目的の言葉だけ。
- **`web/src/wasm/` は生成物だがコミットする**（`npm run build:wasm`）。Rust を触ったら忘れない。
- テストは `cargo test --workspace` と `npm test`（web）。
